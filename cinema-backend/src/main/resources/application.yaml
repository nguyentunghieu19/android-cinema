spring:
  application:
    name: cinema-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/androidcinema
    username: postgres
    password: nguyentunghieu

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080